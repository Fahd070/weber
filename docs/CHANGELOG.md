# Changelog

All notable changes to the Weber project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project intends to adhere to [Semantic Versioning](https://semver.org/) once a first versioned release exists.

No product releases have occurred yet. This file will begin recording versioned entries (`## [1.0.0] - YYYY-MM-DD`, etc.) starting with Weber's first approved release, per the Release Strategy defined in `PROJECT_ROADMAP.md`.

---

## [Unreleased]

### Added

- Established the Weber Project Constitution (Version 1.0) as the project's governing document.
- Created the `docs/` documentation system: `PROJECT_CONSTITUTION.md`, `PROJECT_ROADMAP.md`, `ARCHITECTURE.md`, `DECISIONS.md`, `DEVELOPMENT_GUIDE.md`, `CODING_STANDARDS.md`, `UI_UX_GUIDELINES.md`, `BRAND_GUIDELINES.md`, `CHANGELOG.md`, and `README.md`.
- Ratified Amendment 1 to the Constitution (now Version 1.1): content/component separation, single source of truth for data, AI security decision-authority limits, and a performance-vs-feature trade-off gate. See `DECISIONS.md` (WD-010) and `PROJECT_CONSTITUTION.md` Part 11.
- Approved Weber's core technology stack (Technology Stack Finalization — Phase 2): Next.js (App Router), TypeScript, Tailwind CSS, shadcn/ui on Radix UI, Motion, Lucide React, React Hook Form, Zod, Zustand, next-intl, PostgreSQL, Prisma ORM, and Next.js Route Handlers/Server Actions as the V1 backend architecture. See `DECISIONS.md` WD-014–WD-026 and `ARCHITECTURE.md` §0. Authentication, hosting, CMS, and several other categories remain undecided.
- Created `docs/UI_FOUNDATION.md` — the visual philosophy foundation preceding Design Tokens.
- Approved and documented Weber's full design token system (Design Tokens Finalization): typography scale, font weights, line-height, spacing, border radius, elevation, motion, breakpoints, and container widths. See `DECISIONS.md` WD-027–WD-036 and the new `docs/DESIGN_TOKENS.md`. Semantic color hex values remain pending.
- Synchronized Weber's Version 1 information architecture (IA Decision Synchronization): confirmed the four Version 1 services (Website Development, Mobile Applications, Desktop Applications, Templates), confirmed the Learning Platform as Version 2 only, approved the Blog as a Version 1 content strategy feature, approved the nine-section Home page order including new "Why Weber" and "Process" sections, and officially adopted a multi-route architecture. See `DECISIONS.md` WD-037–WD-043.
- Created `docs/INFORMATION_ARCHITECTURE.md` (Information Architecture Finalization) — the official, consolidated site structure, navigation architecture, page-by-page IA, user journeys, content relationships, SEO structure, and component mapping for Version 1.
- Created `docs/UX_SPECIFICATION.md` (UX Specification Finalization) — the official interaction and experience specification: global interaction states, navigation behavior, forms, the Interactive Showcase journey, loading patterns, feedback system, accessibility, responsive UX, and conversion-stage UX responsibilities. See `DECISIONS.md` WD-044–WD-052.
- Created `docs/DESIGN_SYSTEM.md` (Design System Finalization) — the official Design System specification: component design principles, component philosophy including the new Composition First principle, an eight-category component classification, variant philosophy, cross-surface consistency model, responsive component strategy, accessibility and motion requirements, and future scalability principles. See `DECISIONS.md` WD-053–WD-062.
- Created `docs/components/` (Component Library Architecture Finalization) — the official ten-document Component Library structure (`01_FOUNDATIONS.md` through `10_MASTER_INDEX.md`), including a ten-field component documentation standard (with the new Dependencies field), a six-stage component lifecycle, a binding Documentation Before Implementation gate, naming philosophy, and a clarified dependency layering rule. No individual component is documented yet. See `DECISIONS.md` WD-063–WD-069.
- Documented Weber's first eleven components (Foundations Components Finalization): App Shell, Page, Container, Section, Stack, Grid, Cluster, Divider, and Aspect Ratio (`docs/components/02_LAYOUT.md`), plus Skip Link and Visually Hidden (`docs/components/09_UTILITIES.md`). Clarified that "Foundation Components" is a role within the existing Layout/Utilities categories, not a new category. Spacer and Screen Reader Only excluded as redundant; a Live Region Announcer deferred to a future accessibility phase. See `DECISIONS.md` WD-070–WD-072.
- Synchronized Foundation Component documentation following Component Audit #1: corrected App Shell to a slot-based composition model (resolving a WD-068 layering violation), recorded Grid's and Aspect Ratio's missing token categories as explicit future extension points, and clarified Page's naming against Next.js's `page` convention. No component added, removed, or renamed. See `DECISIONS.md` WD-073.
- Confirmed the Layout component category complete for Version 1 (Layout Category Confirmation): the nine Foundation Layout Components are the final Layout set — sixteen candidate aliases (Header Layout, Footer Layout, Section Layout, Content Wrapper, etc.) were reviewed and explicitly rejected; Empty State and Sidebar Layout were redirected to future phases rather than documented now. See `DECISIONS.md` WD-074–WD-075.
- Documented Weber's Navigation component category (Navigation Components Finalization): Header, Navigation Menu, Navigation Item, Brand Link, Mobile Navigation Toggle, CTA inside Navigation, Breadcrumb (optional), and Language Switcher (`docs/components/03_NAVIGATION.md`). Language Switcher's category (Navigation vs. Utilities) was explicitly resolved; CTA inside Navigation is documented as depending on a future Button component rather than inventing a new CTA primitive. Navigation Bar, Mobile Navigation, Navigation Group, a standalone Logo Component, and Search Trigger were rejected. See `DECISIONS.md` WD-076–WD-079.
- Confirmed Theme Switcher is not approved for Version 1 — dark theme remains the only approved theme (WD-003); deferred until a future theme-expansion phase. Resolves the last open flag from Navigation Components Finalization; the Navigation category is now complete with no open blockers. See `DECISIONS.md` WD-080.
- Documented Weber's Inputs component category (Inputs Components Finalization): Button, Text Field, Text Area (optional), Select, Form, Form Field, Label, Helper Text, Error Message (field-scoped, distinct from Feedback's system-level Error type), and Required Indicator (`docs/components/04_INPUTS.md`). Button resolves the forward dependency recorded against Navigation's CTA element (WD-079) and is confirmed as an Inputs-category component (WD-084). Seventeen candidates were rejected as variants or unjustified complexity (WD-082); Password Field, OTP Input, PIN Input, File Upload, Drag & Drop Upload, and Switch were deferred rather than rejected — Switch specifically because no Version 1 feature requires it yet, not because of the Theme Switcher decision (WD-083). See `DECISIONS.md` WD-081–WD-084.
- Documented Weber's Feedback component category (Feedback Components Finalization): Alert (Success/Warning/Error/Informational variants), Spinner, Skeleton, and Empty State (Default + Error variants, absorbing what would otherwise be a separate "Error State" component) (`docs/components/05_FEEDBACK.md`). Fifteen candidates were rejected as aliases or unjustified complexity; Modal and Confirmation Dialog were redirected to the Surfaces category; Status Badge was deferred to Version 2; Live Region Announcer was explicitly confirmed as a future Utilities component rather than Feedback, per direct Project Owner clarification. See `DECISIONS.md` WD-085–WD-088.
- Documented Weber's Data Display component category (Data Display Components Finalization): Service Card, Portfolio Card, Blog Post Card, Testimonial Card, Tag, Timeline, Accordion, Case Study Layout, Article Layout, Avatar (optional), and Key Value Pair (optional) (`docs/components/06_DATA_DISPLAY.md`). Case Study Layout and Article Layout were explicitly confirmed as Data Display components, not Layout-category primitives, per direct Project Owner clarification. Fifteen candidates were rejected as aliases or unjustified complexity, including Price Display, which directly contradicts the Consultation-First Pricing decision (WD-006); Card was redirected to the future Surfaces category, and Data Table and Progress Metric were deferred to Version 2. Every "X Card" component now carries an open forward dependency on a future Surfaces Card primitive, mirroring the Navigation CTA/Button pattern (WD-079/WD-084). See `DECISIONS.md` WD-089–WD-091.
- Documented Weber's Surfaces component category (Surfaces Components Finalization): Card and Tooltip (`docs/components/07_SURFACES.md`). Card resolves the forward dependency carried by Service Card, Portfolio Card, Blog Post Card, and Testimonial Card since Data Display Finalization (WD-091), completing that dependency chain. Tooltip was explicitly approved as a core Surfaces primitive despite having no Version 1 consumer, per direct Project Owner decision to avoid reopening the category later. Modal, Confirmation Dialog, Sheet, Drawer, Bottom Sheet, Popover, Hover Card, and Side Panel were deferred to Version 2 as a single connected review; Surface (a generic primitive beneath Card), Panel, Overlay, Backdrop, Scroll Area, and Floating Action Surface were rejected; Callout, Separator Surface, and Sticky Container were confirmed to already exist elsewhere (Alert, Divider, Header) with no new documentation needed. See `DECISIONS.md` WD-092–WD-094.
- Documented Weber's Marketing component category (Marketing Components Finalization): Hero, CTA Banner, Feature List, and Preview Frame (`docs/components/08_MARKETING.md`) — the eighth and final Component Library category to receive documentation, per `DESIGN_SYSTEM.md` §4. Preview Frame was identified as a required addition during the Phase 1 review, not on the original candidate list, grounded directly in `INFORMATION_ARCHITECTURE.md` §15. Hero Content and Hero Media were confirmed as internal compositions of Hero (Stack and Aspect Ratio), not independent components. Blog Preview was documented as a named composition pattern — Section + Grid + Blog Post Card + CTA Banner — rather than rejected outright or documented as a standalone primitive, since it has no approved Version 1 placement but remains a valid, reusable recipe. Seven candidates were rejected as redundant page-level compositions, seven more for lacking any approved or plausible use case (including Pricing Section, which contradicts WD-006), and three were confirmed to belong to already-documented categories. A pre-existing documentation gap was flagged: Footer has never been documented as a component despite being referenced in App Shell and Navigation decisions. See `DECISIONS.md` WD-095–WD-097.
- Documented Weber's remaining Utilities components (Utilities Components Finalization): Icon and Live Region Announcer, joining the already-documented Skip Link and Visually Hidden (`docs/components/09_UTILITIES.md`). Both close the two most-referenced open dependencies in the entire Component Library — Icon resolves forward dependencies from Mobile Navigation Toggle (Navigation), Button (Inputs), Empty State (Feedback), and Feature List (Marketing); Live Region Announcer resolves forward dependencies from Alert, Spinner, Empty State (Feedback), Form (Inputs), and Preview Frame (Marketing), fulfilling the category commitment made in WD-088. All five affected documents were updated to reference the now-documented primitives. Screen Reader Announcer was confirmed as an alias of Live Region Announcer rather than a separate component. Footer was confirmed to belong to Navigation, not Utilities, and remains undocumented pending a future Navigation documentation pass. Focus Trap remains deferred to Version 2, tied to the already-deferred Surfaces overlay family. Ten further candidates (Theme Provider, Theme Switcher helper, Keyboard Shortcut Helper, Copy to Clipboard, External Link Indicator, Loading Boundary, Error Boundary, Client Only, No SSR Wrapper, Portal) were rejected as framework-specific mechanisms or ungrounded ideas. See `DECISIONS.md` WD-098–WD-100.
- Created `docs/SECURITY_POLICY.md` (Security Policy Finalization) — the official security specification referenced but never created since Technology Stack Finalization. Nine topics (security-is-a-requirement, input validation, SQL injection prevention, secrets management, environment variables, dependency management, third-party packages, and error handling) are documented as approved principles, directly restating existing Constitution and `DECISIONS.md` text. A dedicated "Security Decision Boundaries" section explicitly states the document does not define or imply an authentication mechanism, authorization model, session strategy, or encryption approach until each is separately approved, reinforcing the AI Security Decision Authority Rule (WD-010). Eleven further topics (including output encoding/XSS, CSRF, security headers, CSP, rate limiting, and file upload security) are recorded as pending Project Owner decisions, and four (infrastructure-level controls, email provider security, CMS access control, payment/billing security) are recorded as out of scope, the last because Consultation-First Pricing (WD-006) means no payment flow exists at all. `ARCHITECTURE.md` and `PROJECT_STRUCTURE.md` were updated to reflect that this long-referenced file now exists. See `DECISIONS.md` WD-101–WD-103.
- Created `docs/PERFORMANCE_GUIDE.md` (Performance Guide Finalization) — the last of the two files `ARCHITECTURE.md` had referenced but never had written. Seven areas (performance philosophy, optimization targets, loading strategy, rendering strategy, code splitting, animation performance, and the resolved relationship between accessibility and performance) are documented as approved principles, directly restating existing Constitution, `DECISIONS.md`, and engineering-standards text; naming an optimization target (images, fonts, bundles, network requests) is explicitly distinguished from approving a technique for it. A dedicated "Performance Decision Boundaries" section, mirroring `SECURITY_POLICY.md`'s pattern, explicitly states the document does not define or imply performance budgets, Core Web Vitals targets, caching strategy, monitoring tooling, database optimization strategy, or API optimization strategy until each is separately approved. Ten further topics are recorded as pending Project Owner decisions, two (hosting-specific and CMS-specific performance features) as out of scope, and Client Dashboard/Learning Platform performance needs as Version 2. `ARCHITECTURE.md` and `PROJECT_STRUCTURE.md` were updated to reflect that both long-referenced files now exist. See `DECISIONS.md` WD-104–WD-106.
- Created `docs/IMPLEMENTATION_BLUEPRINT.md` (Implementation Blueprint Finalization) — converts the fifty-two documented components and every approved architecture/policy document into a dependency-driven execution sequence, without inventing any new architecture. Establishes a nine-step Critical Path, a fourteen-area Build Order table, parallel-implementation opportunities, and a list of components/pages to delay. Confirms Milestone/Phase-based as the implementation strategy — the cadence the Constitution's Phase Execution Framework already mandates, not a new choice — with Feature-based code organization applied within each milestone and Sprint-based explicitly rejected as unsupported by any approved documentation. A dedicated "Implementation Readiness Checklist" summarizes project readiness across seven areas, concluding Weber is ready for Tier 1 implementation now but not for full page assembly, launch, or deployment. Surfaced two previously unresolved documentation gaps during synthesis: Footer (Navigation) and Service Detail Layout (never proposed in any Component Library workshop, unlike its Portfolio and Blog counterparts). See `DECISIONS.md` WD-107–WD-109.
- Documented Footer as Navigation's ninth component (Footer Component Finalization): a composition-only landmark filling App Shell's Footer slot, structurally parallel to Header (`docs/components/03_NAVIGATION.md`). Footer reuses Brand Link rather than duplicating it; Language Switcher is explicitly not composed into Footer by default, keeping its placement independent of language strategy. Footer has exactly one global variant and is non-sticky, appearing once per page, consistent with WD-045. No Footer content — links, trust signals, Support Pages tier items, or legal pages — is approved; all remain `[PENDING PROJECT OWNER DECISION]`. This closes the last piece of global chrome, touching all six Version 1 routes, that had no documented component behind it. Updated `docs/components/02_LAYOUT.md` (App Shell), `docs/components/09_UTILITIES.md`, `docs/components/10_MASTER_INDEX.md`, `docs/DESIGN_SYSTEM.md`, and `docs/IMPLEMENTATION_BLUEPRINT.md` to reflect the resolution. See `DECISIONS.md` WD-110–WD-111.
- Documented Service Detail Layout as Data Display's tenth component (Service Detail Layout Finalization), closing the last content-presentation gap on record (`docs/components/06_DATA_DISPLAY.md`). Follows the exact architectural philosophy already established for Case Study Layout and Article Layout — composition only, no new primitives — composing only Section, Container, and Stack, closer in shape to Article Layout's simplicity than Case Study Layout's fact-grid. CTA Banner is explicitly excluded as a dependency, since Data Display (tier 3) may never depend on Marketing (tier 4) under WD-068; it is a page-level composition instead, the same relationship Case Study Layout and Article Layout already have with CTA Banner, now made explicit. Cross-links to Portfolio (named in `INFORMATION_ARCHITECTURE.md` §15) are documented as an optional, page-owned content area rather than a component dependency. Corrected a pre-existing citation error in Service Card's own entry, which had cited `INFORMATION_ARCHITECTURE.md` §8 (Portfolio Architecture) instead of §6 (Services) for Services-related content. Updated `docs/components/10_MASTER_INDEX.md`, `docs/DESIGN_SYSTEM.md`, and `docs/IMPLEMENTATION_BLUEPRINT.md` — the Implementation Readiness Checklist's Component Library row now reads fully complete, with no content-presentation component gap remaining. See `DECISIONS.md` WD-112–WD-113.
- Adopted pnpm as Weber's official package manager (Package Manager Finalization), resolving the Critical Bootstrap blocker identified during the Bootstrap Foundation Workshop. Chosen against Weber's already-approved engineering principles — long-term maintainability, Scalability toward a possible future Client Dashboard/Learning Platform workspace split, and the Single Source of Truth discipline already established elsewhere — with npm presented as the legitimate, lower-risk alternative rather than dismissed. No other Bootstrap-level decision (Node.js version, `create-next-app` configuration, physical project structure, tooling, scripts, Git workflow) is resolved by this decision; each remains open for its own future workshop. Updated `ARCHITECTURE.md` §0 (Approved Technology Stack table) and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Bootstrap readiness row. See `DECISIONS.md` WD-114.
- Adopted a Node.js version policy (Node.js Version Policy Finalization), explicitly separated into three layers: Policy — Active LTS, with future updates triggered only by a Next.js major upgrade or an Active LTS transition, never a calendar schedule; Concrete Version — deliberately left open, since it is a dependent value determined only once Weber's still-unpinned Next.js version is officially pinned; Enforcement Mechanism — `package.json` `engines` and `.nvmrc` both adopted, with their version values remaining open alongside the concrete version itself. Mirrors the same approve-the-rule-defer-the-value pattern already used for semantic color roles/values (WD-036) and the Security/Performance Decision Boundaries (WD-102, WD-105). Updated `ARCHITECTURE.md` §0 and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Bootstrap readiness, Critical decisions, and Remaining open decisions rows. See `DECISIONS.md` WD-115.
- Resolved Weber's `create-next-app` bootstrap configuration (Create Next App Configuration Finalization), separated into four strictly distinct layers. Bootstrap configuration: TypeScript, Tailwind, and App Router restated as already-resolved (WD-014–016); `src/` directory adopted and `@/*` import alias confirmed as the two coupled, previously-open architectural decisions — both resolved by direct Project Owner decision, not an AI default, after a clarifying question. Tooling policy: ESLint installed as a Bootstrap tool only, using Next.js's baseline config; Turbopack enabled for `next dev` only. ESLint rule configuration remains explicitly separate and undecided. React Compiler and production Turbopack remain explicitly deferred as version-dependent, pending a pinned Next.js version. Updated `ARCHITECTURE.md` §0 and §2, `CODING_STANDARDS.md`, `DEVELOPMENT_GUIDE.md`, and `docs/IMPLEMENTATION_BLUEPRINT.md` — Bootstrap readiness now reads Ready to scaffold. See `DECISIONS.md` WD-116.
- Approved Weber's physical project structure beneath `src/` (Project Structure Finalization), added as `PROJECT_STRUCTURE.md` §11 — the first section in that document to use real directory names. Adopted a **Hybrid** organizational model: the top level follows the four already-approved conceptual layers, while `features/` is organized feature-first internally, per the already-approved feature-based organization principle. Six directories are required: `app/`, `components/` (mirroring the Component Library's eight documented categories), `features/`, `content/`, `lib/`, and `providers/` (scoped narrowly to next-intl). Nine directories — `store/`, `services/`, `middleware.ts`, `utils/`, `config/`, `constants/`, `styles/`, `assets/`, and `types/` — are recorded as intentionally not created now, each with its own documented rationale, to prevent silent re-litigation later. Three directories — `actions/`, `server/`, and `shared/` — are rejected outright as duplicating responsibilities already owned by `features/` and `lib/`. Three Version 2 directory deferrals (a Client Dashboard feature folder, an authentication directory, a versioned API structure) are recorded separately, since each depends on an entire unscoped future system rather than a resolvable Version 1 condition. Updated `ARCHITECTURE.md` §0, §2, §3, and §5, and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Critical decisions, Remaining open decisions, Known implementation blockers, Bootstrap readiness, and Build Order rows. See `DECISIONS.md` WD-117–WD-120.
- Adopted Weber's ESLint rule policy (ESLint Rules Finalization), completing the rule-set layer WD-116 explicitly left open from tool installation. Philosophy: correctness-first and maintainability-first, at a moderate rule set — not minimal, not maximal. Eight rule categories approved: accessibility (`eslint-plugin-jsx-a11y`), React, React Hooks, the base `@typescript-eslint` plugin, `no-explicit-any`, `no-debugger`, `prefer-const`, and `no-var`, each traced to a specific Constitution or `CODING_STANDARDS.md` principle rather than popularity. Import-boundary enforcement is approved as the architectural *direction* for enforcing WD-068's dependency-tier layering, with the concrete plugin and configuration explicitly left as a separate future tooling decision. `complexity`, `max-lines`, `max-params` numeric thresholds and mechanical naming-convention enforcement are explicitly deferred, since no specific value has ever been approved. ESLint, TypeScript, and Prettier responsibilities are kept strictly separate — Prettier configuration and TypeScript compiler strictness remain entirely undecided. Updated `docs/DEVELOPMENT_GUIDE.md`, `ARCHITECTURE.md` §0, and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Critical decisions, Remaining open decisions, and Bootstrap readiness rows. See `DECISIONS.md` WD-121.
- Adopted Prettier as Weber's formatting tool (Prettier Finalization), resolving the last "still open" tooling item named since the original Bootstrap Workshop. Formatting philosophy: "Defer to Prettier defaults unless an approved Weber principle requires otherwise," extending WD-121's non-maximalist discipline into formatting. Trailing commas, JSX formatting, LF line endings, and end-of-file newline are approved formatting categories, each traced to a specific Weber concern rather than popularity. Print width, quote style, semicolons, and tab width remain explicitly deferred — print width in particular mirrors WD-121's own numeric-threshold discipline exactly, since accepting Prettier's default silently would break that just-established precedent. Automatic formatting of `docs/` is explicitly **not** approved and remains excluded by default, since Weber's governance documentation is intentionally maintained manually through the Workshop → Finalization cycle. Import ordering is explicitly reaffirmed as ESLint's responsibility, not Prettier's. Updated `docs/DEVELOPMENT_GUIDE.md`, `ARCHITECTURE.md` §0, and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Critical decisions, Remaining open decisions, and Bootstrap readiness rows. See `DECISIONS.md` WD-122.
- Adopted EditorConfig (EditorConfig Finalization), scoped narrowly to genuine editor-level territory — the earliest stage in Weber's formatting pipeline, ahead of Prettier, ESLint, and TypeScript. `root=true`, `charset=utf-8`, and a Markdown-specific whitespace-protection section (guarding the two-space hard-line-break convention `docs/` relies on) are approved. `end_of_line=lf` and `insert_final_newline=true` are recorded explicitly as reflections of WD-122's already-approved values, not independent decisions, preventing EditorConfig from becoming a second, drifting source of formatting policy. `indent_size`, `tab_width`, `indent_style`, quote style, semicolons, print width, and JSON/YAML formatting all remain undecided. Updated `docs/DEVELOPMENT_GUIDE.md`, `ARCHITECTURE.md` §0, and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Critical decisions, Remaining open decisions, and Bootstrap readiness rows. See `DECISIONS.md` WD-123.
- Adopted Husky as Weber's Git hook orchestration tool (Husky Finalization), resolving the last item named repeatedly as "still open" since WD-116. `pre-commit` is the only approved hook, scoped strictly to orchestrating already-approved ESLint (WD-121) and Prettier (WD-122) policy — its lint-staged payload remains a separate, undecided item. `commit-msg`, `pre-push`, `post-merge`, and `post-checkout` are all explicitly deferred, each against its own named blocking decision (commit message convention, testing framework and branch strategy, and no urgent grounding, respectively) — not rejected, but genuinely unable to be configured without inventing the missing prerequisite. Responsibility boundaries are reaffirmed explicitly: Husky decides when tooling runs; lint-staged decides what runs and which files are affected; ESLint, Prettier, EditorConfig, and TypeScript each retain their own already-decided policy untouched. Husky is explicitly confirmed as not a Bootstrap requirement. Updated `docs/DEVELOPMENT_GUIDE.md`, `ARCHITECTURE.md` §0, and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Critical decisions, Remaining open decisions, and Bootstrap readiness rows. See `DECISIONS.md` WD-124.
- Adopted lint-staged (lint-staged Finalization), completing the `pre-commit` payload WD-124 deliberately left open — the last item in Weber's formatting/linting/hook pipeline (EditorConfig → Prettier → ESLint → Husky → lint-staged). Scoped to exactly two commands, both already named by WD-124's own closed authorization: ESLint and Prettier on staged files, with `docs/` remaining excluded exactly as WD-122 already requires. Type checking, testing, build execution, generated files, Prisma-specific behavior, `package.json`-specific behavior, and image optimization are all explicitly deferred, each against its own named prerequisite (testing framework, CI platform, or an as-yet-undefined convention) rather than invented. The seven-way responsibility chain is reaffirmed explicitly, now including CI: Husky decides when hooks execute; lint-staged decides what runs on staged files; ESLint, Prettier, EditorConfig, and TypeScript each retain their own already-decided policy; CI performs repository-wide verification, itself still entirely undecided. lint-staged completes the already-approved `pre-commit` pipeline rather than extending it, is recommended for Bootstrap completion, and is confirmed as not a Bootstrap blocker. Updated `docs/DEVELOPMENT_GUIDE.md`, `ARCHITECTURE.md` §0, and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Critical decisions, Remaining open decisions, and Bootstrap readiness rows. See `DECISIONS.md` WD-125.
- Adopted a Next.js version policy (Next.js Version Policy Finalization), resolving the Critical Bootstrap blocker the Node.js Version Policy (WD-115) and Create Next App Configuration (WD-116) Finalizations had each named against it. Explicitly separated into four layers: Version Policy — **Active Major, deliberately pinned, never floating automatically**, since Next.js has no official LTS program (an external technical fact distinguishing it from Node.js's Active/Maintenance/EOL tiers), making a literal "LTS" label a category error; Concrete Version — explicitly deferred, since naming one now would mean assuming either "newest" or "old and stable," both rejected as ungrounded defaults; Upgrade Policy — **event-driven, never calendar-driven**, mirroring WD-115's own upgrade philosophy directly; Responsibility Boundaries — this decision does not choose a concrete Next.js version, does not choose React Compiler, does not choose production Turbopack, and does not choose the concrete Node.js version, but mechanically unblocks WD-115's Node.js version formula and makes React Compiler and Production Turbopack evaluable, neither approved. Updated `docs/DEVELOPMENT_GUIDE.md`, `ARCHITECTURE.md` §0, and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Critical decisions, Remaining open decisions, and Bootstrap readiness rows. See `DECISIONS.md` WD-126.
- Adopted **Next.js 16** as Weber's concrete Next.js version (Concrete Next.js Version Finalization), resolving the single layer WD-126 deliberately left open. Chosen on technical evidence: Next.js 15's active security-backport window closes within roughly three months of this decision while Next.js 14 is already past support, whereas Next.js 16 is the current actively-supported major with a stable Turbopack production build path and a stable, opt-in React Compiler — and since Weber has no existing Next.js installation, none of the 15→16 breaking changes carry any migration cost. Explicitly distinguished from WD-126's unchanged Version Policy and Upgrade Policy layers. Mechanically enables — without deciding — the concrete Node.js version lookup (WD-115's Active LTS formula, now runnable against Next.js 16's published Node.js ≥ 20.9.0 minimum), the `package.json` `engines` value, and the `.nvmrc` value; makes React Compiler and Production Turbopack evaluable. Does **not** approve React Compiler or Production Turbopack. Updated `docs/DEVELOPMENT_GUIDE.md`, `ARCHITECTURE.md` §0, and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Critical decisions, Remaining open decisions, and Bootstrap readiness rows. See `DECISIONS.md` WD-127.
- Adopted **Node.js 24** as Weber's concrete Node.js version (Concrete Node.js Version Finalization), resolving the single layer WD-115 deliberately left open — now runnable because WD-127 supplied the missing input. Node.js 24 is the current Active LTS line, the mechanical output of running WD-115's already-approved Active LTS formula against Next.js 16's published Node.js ≥ 20.9.0 minimum: Node.js 20 (the literal floor) had already reached end-of-life, Node.js 22 is Maintenance LTS only, and Node.js 24 independently clears pnpm's own Node ≥ 22 requirement as well. Explicitly distinguished from WD-115's unchanged Version Policy and Upgrade Policy layers. Mechanically resolves `package.json` `engines`, `.nvmrc`, the local development runtime, and the CI runtime baseline — each a direct write-through of this one value, introducing no new architectural policy. Does **not** approve React Compiler, does **not** approve Production Turbopack, and does **not** change the Next.js version policy, the concrete Next.js version, the Package Manager, or any other Bootstrap configuration. Updated `docs/DEVELOPMENT_GUIDE.md`, `ARCHITECTURE.md` §0, and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Critical decisions, Remaining open decisions, and Bootstrap readiness rows. See `DECISIONS.md` WD-128.
- Resolved every genuinely open item identified in the Design Foundation Workshop review (Design Foundation Finalization), completing Weber's visual design system: a six-step neutral gray scale interpolated between the already-approved Background and Primary colors rather than a new hue (WD-129); semantic color hex values completing WD-036 — Success, Warning, Danger, Information, and a Neutral role that reuses the already-approved Accent color rather than introducing a fifth hue (WD-130); Font family — Geist Sans/Mono, already integrated via `next/font` at zero additional cost, resolving the `BRAND_GUIDELINES.md` placeholder (WD-131); a shadow-rendering strategy completing WD-032 — no `box-shadow` below Overlay, background-lightness + border instead (WD-132); a five-token z-index scale sized to actually-approved V1 components, not the deferred Version 2 Modal/Sheet/Popover family (WD-133); and the CSS Variables strategy — Tailwind v4's native `@theme` block in `globals.css` as the single source of truth, resolving the "Design Token ↔ Tailwind integration mechanism" gap flagged since the Bootstrap Workshop, plus the full token-to-Tailwind mapping table, including two findings that WD-030's spacing scale and WD-034's breakpoints already match Tailwind v4's own defaults with no custom tokens needed (WD-134). This is a documentation Finalization only — `globals.css` has not yet been edited to reflect the new tokens. Updated `DESIGN_TOKENS.md`, `BRAND_GUIDELINES.md`, `UI_FOUNDATION.md`, `ARCHITECTURE.md` §8, `PROJECT_STRUCTURE.md` §11.3, and `IMPLEMENTATION_BLUEPRINT.md`'s Critical decisions, Remaining open decisions, Known implementation blockers, Critical Path, and Build Order rows. See `DECISIONS.md` WD-129–WD-134.
- Adopted **clsx + tailwind-merge** as Weber's official class name composition and conflict-resolution strategy (Class Name Composition Strategy Finalization), resolving the gap found in the Foundation Components Finalization: plain string concatenation cannot guarantee a consumer's `className` reliably overrides a component's own default Tailwind utility classes, since Tailwind utilities carry equal CSS specificity. Explicitly separated three previously-conflated concerns: class composition (clsx) and conflict resolution (tailwind-merge) are resolved; variant management (class-variance-authority or an alternative) is evaluated and explicitly **deferred, not rejected**, until a component with genuine multi-dimensional variants (most likely Button) is next approved — the same ahead-of-need restraint already applied to `store/` (WD-119) and the Surface primitive (WD-092/WD-094). classnames was rejected on technical grounds (identical function to clsx at several times the bundle size), not popularity. Composed together as a single `cn()` helper, matching the exact convention WD-017's already-approved shadcn/ui would itself generate — not an independent new pattern. Documentation Finalization only: neither package is installed and no project code was modified. Updated `ARCHITECTURE.md` §0, `CODING_STANDARDS.md`, and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Critical decisions, Remaining open decisions, and Known implementation blockers rows. See `DECISIONS.md` WD-135.
- Adopted **Primary / Secondary / Accent** as Button's official Version 1 visual variant set, filled-only, default `primary` (Button Visual Variants Finalization), resolving the sole remaining architectural blocker preventing Button implementation. Reached by evaluating eleven candidates individually against WD-057 (Variant Philosophy) and Weber's documented Version 1 scope: Destructive, Success, and Warning were found unsupported, since no Version 1 route involves a destructive action or needs a button whose resting color communicates an outcome before it's pressed; Ghost and Outline were found to contradict WD-057 outright, since fill-style treatments are not color roles; Link was found explicitly deferred to a future version by Button's own existing documentation. Primary/Secondary/Accent are the only three variants named together, by word, in Button's own pre-existing Variants field and in WD-002. Updated `docs/components/04_INPUTS.md` (Button — Variants field), `DESIGN_SYSTEM.md` (Pending Decisions Summary), and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Critical decisions row. See `DECISIONS.md` WD-136.
- Confirmed **Grid as a value-agnostic layout mechanism** (Grid Mechanism Finalization), resolving the sole remaining architectural blocker preventing Grid's own implementation. Grid provides responsive column-arrangement infrastructure — built on WD-034's breakpoint tiers and WD-030's gutter scale — but defines no default or built-in column-count values; each consuming component (Service Card, Portfolio Card, Blog Post Card, a future Dashboard grid) decides its own counts at its own implementation time. Reached by evaluating five candidate strategies against seven criteria: a fixed universal count was rejected as contradicting WD-034's mobile-first mandate; Grid hardcoding its own per-breakpoint scale was rejected as explicitly forbidden by Grid's own documentation; CSS `auto-fit`/`minmax` was found technically sound but rejected as an unprecedented second responsive paradigm inconsistent with WD-034's tier model used everywhere else; a new Design Tokens grid-column category was rejected as premature, explicitly framed by Grid's own text as a future evaluation. This mechanism-only strategy is close to a direct restatement of Grid's own pre-existing Composition field. Unblocks Grid and, by extension, Case Study Layout; does not itself resolve Service Card's, Portfolio Card's, or Blog Post Card's actual column counts, each remaining its own small, separate decision. Updated `docs/components/02_LAYOUT.md` (Grid — Variants, Token Usage, Future Extension fields), `ARCHITECTURE.md` §5, and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Critical decisions and Known implementation blockers rows (the latter also corrected a stale note — the six original Foundation Components were migrated to `cn()` under WD-135's implementation and no longer carry string-concatenation debt). See `DECISIONS.md` WD-137.
- Implemented **Grid** (`src/components/layout/grid.tsx`) and **Case Study Layout** (`src/components/data-display/case-study-layout.tsx`), applying WD-137's Grid Mechanism Finalization in code. Grid accepts a required, per-breakpoint `cols` prop (no default column count, per WD-137) and renders it via static Tailwind arbitrary-value classes driven by CSS custom properties, so the same class list works for any consumer-supplied count without capping it to a fixed utility scale; gutters reuse the existing `GAP_CLASS` spacing-scale map already established by Stack and Cluster. Case Study Layout composes Section, Container, Stack, Grid, and Tag — all pre-existing — with an optional Grid-arranged facts region left as generic content, since Key Value Pair itself remains undocumented at the implementation level. Portfolio Card and Blog Post Card remain blocked on Aspect Ratio, which has not itself been through its own Workshop/Finalization; Service Card remains open pending its own column-count choice at implementation time. No `DECISIONS.md` entry was required — no new architectural decision was made; WD-137 already governs Grid's contract in full. While reconciling documentation against the actual source tree, corrected the Component Library's per-category and Master Index status claims, which had stated no component had entered Implementation: forty-three of fifty-four components have in fact entered Implementation, most prior to this entry. Updated `docs/components/02_LAYOUT.md`, `03_NAVIGATION.md`, `04_INPUTS.md`, `05_FEEDBACK.md`, `06_DATA_DISPLAY.md`, `07_SURFACES.md`, `08_MARKETING.md`, `09_UTILITIES.md`, and `10_MASTER_INDEX.md` (full per-component Lifecycle Status sync) and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Critical Path and Known implementation blockers rows accordingly.
- Implemented **Service Card** (`src/components/data-display/service-card.tsx`), completing Critical Path step 5 aside from the two components still blocked on Aspect Ratio. Composes the Surfaces Card primitive and reuses Button's visual treatment as a real link via `buttonVariants` — the exact pattern already established by CTA inside Navigation — rather than a native `<button>`, since Button itself does not own link/navigation logic. The entry point's accessible name includes the service name via the existing VisuallyHidden primitive, never a generic "Learn more" alone, per the component's own documented Accessibility requirement. Clarified during implementation that Service Card does not itself compose Grid — it is a single grid item, not a Grid container — so the column-count question WD-137 raised for it belongs to whatever eventually assembles a listing of Service Cards (Home Services section or Services page), not to this component; it carried no real blocker once Card, Grid, and Button existed. No `DECISIONS.md` entry required. Updated `docs/components/06_DATA_DISPLAY.md` and `10_MASTER_INDEX.md` (Lifecycle Status) and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Critical Path (steps 5–6) and Known implementation blockers rows — the latter now also names Contact route assembly's actual blocker (its lead-collection field list remains an open Project Owner decision).
- Implemented **Select** (`src/components/inputs/select.tsx`), closing the last gap in the Tier 2 Inputs-primitives parallel-implementation track named in `IMPLEMENTATION_BLUEPRINT.md` §6 (Text Field, Label, Helper Text, and Required Indicator were already done). Renders a native `<select>` rather than a Radix-based custom listbox — no other implemented component in this codebase installs or uses a Radix package despite WD-017 approving shadcn/ui on Radix, so adding one for a single component would be a new, unjustified dependency under `CODING_STANDARDS.md`'s Performance Rules; the platform-native element also satisfies the component's keyboard/AT accessibility requirements more robustly than a hand-built alternative, at the cost of not being able to style its dropdown's open/close transition with Weber's motion tokens. Styling copied verbatim from Text Field's class list, since both are same-weight single-value form primitives. No `DECISIONS.md` entry required — no new architectural decision was made. `IMPLEMENTATION_BLUEPRINT.md`'s Critical Path (§4) does not name Select directly, so it was not modified; only `docs/components/04_INPUTS.md` and `10_MASTER_INDEX.md` were updated (Lifecycle Status).
- Confirmed **Aspect Ratio as a value-agnostic layout mechanism** (Aspect Ratio Mechanism Finalization), resolving the sole remaining Foundation Component blocker (WD-071) — directly mirroring Grid's WD-137 resolution. Aspect Ratio reserves space for a content region at a caller-supplied ratio, preventing layout shift, but defines no built-in ratio values, no ratio enum, no default ratio, and no Design Tokens aspect-ratio category. Every consumer — Portfolio Card, Blog Post Card, Hero, Preview Frame, and any future image-heavy component — supplies its own ratio at its own implementation time. Reached by evaluating six candidate strategies (fixed enum, free numeric API, token-based system, consumer-defined ratio, CSS-only approach, responsive/per-breakpoint API) against eight governing-principle criteria: a fixed enum was rejected as contradicting Variant Philosophy (WD-057), which requires variants to map to approved tokens that don't exist for ratios; a token-based system was rejected as premature, mirroring WD-137's identical finding for Grid's column-count tokens; a CSS-only approach was rejected as out of scope, since Aspect Ratio is already an approved Foundation Component; a responsive/per-breakpoint API was rejected as solving a need no document actually states. The consumer analysis found Portfolio Card, Blog Post Card, Hero, and Preview Frame each need exactly one ratio for themselves but plausibly different values from each other, and that Preview Frame's specific ratio may not even be resolvable until the Interactive Showcase generation mechanism is itself decided — reinforcing that no value should be fixed at the mechanism level now. This is a documentation Finalization only — no code was written. Updated `docs/components/02_LAYOUT.md` (Aspect Ratio — Purpose, Variants, Token Usage, Relationships, Dependencies, Future Extension fields; also corrected a pre-existing incompleteness found during the Workshop's consumer analysis — Hero and Preview Frame were never listed as consumers alongside Portfolio Card and Blog Post Card, despite both composing Aspect Ratio per `08_MARKETING.md`), `docs/components/10_MASTER_INDEX.md` (Aspect Ratio, Portfolio Card, Blog Post Card status), and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Critical Path (steps 2 and 5) and Known implementation blockers row. See `DECISIONS.md` WD-138.
- Approved **Name, Company, Email, and Message** as Weber's Version 1 Contact Form field list, all four required with no optional fields (Contact Form Field Definition Finalization), resolving the "Contact lead-collection field list" gap left open since Information Architecture Finalization. Reached by evaluating six candidate strategies (Minimal Lead Form, Standard Agency Form, Qualification Form, Multi-step Form, Service-first Form, Custom Proposal Form) against eight governing principles, grounded only in already-approved documentation: Name and Email are the functional minimum for "start a consultation" to mean anything; Message traces directly to WD-007's own approved CTA copy ("Tell us about your business"); Company is required, not speculative, because every route's Primary Audience is identically "Businesses seeking a professional digital presence" — Weber has no approved individual-consumer segment. Budget was rejected as directly contradicting Consultation-First Pricing (WD-006), the same failure mode already rejected for Price Display (WD-090). Attachment was rejected as depending on the still-undecided File Upload Strategy. Phone, Timeline, Project Type, Website, Country, and Preferred Contact Method were found unsupported or merely speculative. Service is confirmed as a possible future pre-fill value from a Services-page referral, not a Contact field itself. This entry resolves only the field list — ordering, copy, validation messages, and the submission mechanism (Server Actions vs. Route Handler) remain implementation-level work under already-approved decisions (WD-020, WD-021, WD-026, WD-046). Updated `docs/INFORMATION_ARCHITECTURE.md` §10 and its Pending Decisions Summary, and `docs/IMPLEMENTATION_BLUEPRINT.md`'s Remaining open decisions row, Known implementation blockers row, Critical Path step 6, and Contact implementation order row. See `DECISIONS.md` WD-139.
- Approved **"Explore Our Services," linking to `/services`,** as the canonical Version 1 Home Hero CTA (Home Hero CTA Finalization), resolving the gap flagged during Home Hero Section implementation — Hero's own documentation says it leads "toward Services and Interactive Showcase," but `ROUTES.md` §6 classifies all three of WD-007's approved CTA phrases as Contact-directed language, and separately marks the "Navigate to Services" functional role's exact label as undecided. Directing the Hero CTA to Contact this early would have contradicted Progressive Trust ("do not ask users to contact Weber immediately," `PROJECT_CONSTITUTION.md` Part 4); "Explore Our Services" instead invites exploration, consistent with Consultation-First Pricing (WD-006) not front-loading a consultation ask. A plain Button was chosen over a CTA Banner as the simpler of Hero's two already-approved CTA options. Scoped narrowly: this decides only the Home Hero's own CTA, not any other "Navigate to Services" or "Navigate to Interactive Showcase" instance elsewhere in the product. This is a documentation Finalization only — no code was written. Updated `docs/ROUTES.md` §6 (CTA Architecture table and its following note) and Pending Decisions Summary. See `DECISIONS.md` WD-140.

---

## Changelog Conventions

Once versioned releases begin, each entry should use the following categories as needed:

- `Added` — new features.
- `Changed` — changes to existing functionality.
- `Deprecated` — features that will be removed in an upcoming release.
- `Removed` — features removed in this release.
- `Fixed` — bug fixes.
- `Security` — vulnerability fixes.

Every entry should be traceable to an approved decision in `DECISIONS.md` where applicable.
